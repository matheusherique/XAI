version: '3'
services:
  tensorflow:
    image: tensorflow/tensorflow:2.3.2-gpu-jupyter
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
    ports: 
      - 8888:8888
    volumes: 
      - .:/tf
